/*
 * @package    oakcms
 * @author     Hryvinskyi Volodymyr <script@email.ua>
 * @copyright  Copyright (c) 2015 - 2017. Hryvinskyi Volodymyr
 * @version    0.0.1-alpha.0.4
 */
$(document).ready(function () {
    //var $fbEditor = $('.build-wrap'),
    //    formBuilder = $fbEditor.formBuilder({dataType: 'json', formData: formBuilderData}).data('formBuilder');

    console.log($.parseJSON(formBuilderData));

    /**$(".form-builder-save").click(function(e) {
        e.preventDefault();
        $.post($fbEditor.data('url'), {data: formBuilder.formData}).done(function( data ) {
            if(data.result = 'success') {
                grow(data.success, 'success');
            } else {
                grow(data.error, 'error');
            }
        });
    });*/
});

